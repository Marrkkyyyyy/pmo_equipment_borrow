import 'package:flutter/material.dart';

/// A class that defines a color palette for the PMO Equipment and Office Supplies Inventory System.
class AppColor {
  // Primary Colors
  static const Color navyBlue = Color(0xFF003366); 
  static const Color tealGreen = Color(0xFF008080);

  // Secondary Colors
  static const Color lightGray = Color(0xFFF5F5F5); 
  static const Color darkGray = Color(0xFF4A4A4A);
  static const Color skyBlue = Color(0xFF00BFFF); 

  // Alert Colors
  static const Color green = Color(0xFF28A745); 
  static const Color yellow = Color(0xFFFFC107); 
  static const Color red = Color(0xFFDC3545); 

  // Background and Card Colors
  static const Color white = Color.fromRGBO(249, 247, 247, 1.0); 
  static const Color lightBlue = Color(0xFFE6F7FF); 

  // User Page Colors (from the new palette)
  static const Color userLightGray = Color(0xFFDDDDDD); // Light Gray for backgrounds
  static const Color userDarkGray = Color(0xFF222831);  // Dark Gray for primary text or backgrounds
  static const Color userDeepBlue = Color(0xFF30475E);  // Deep Blue for headers or accents
  static const Color userSoftRed = Color(0xFFF05454);   // Soft Red for action buttons or highlights
}
